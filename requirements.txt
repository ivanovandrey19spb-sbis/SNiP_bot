# MVP: bot.py + RAG из Supabase (rag.py)
# NOTE: модуль `re` — часть стандартной библиотеки Python, его нельзя закрепить через pip.
# Чтобы "зафиксировать версию re", фиксируйте версию Python (для этого проекта требуется Python>=3.10).
supabase==2.15.3
python-dotenv==1.0.0
pyTelegramBotAPI==4.32.0

# LLM (OpenRouter/OpenAI-compatible) — без langchain-* (конфликт с langgraph на общих ВМ)
openai==2.32.0

# RAG: Qdrant + HF embeddings (для ai-forever/ru-en-RoSBERTa)
qdrant-client==1.9.0
numpy==2.2.5
transformers==5.5.4
torch==2.11.0

# RAG lexical: generic RU stopwords для Supabase или_ ilike
stop-words==2018.7.23



#psycopg2==2.9.11
#sentence-transformers==5.3.0
#pypdf==3.17.0
#tqdm==4.66.1
#langchain_text_splitters==1.1.1
